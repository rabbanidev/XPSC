#include <bits/stdc++.h>
#define el "\n"

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // pair<string, int> student = make_pair("Rabbani", 100);
    // pair<string, int> student = {"Rabbani", 100};
    // cout << student.first << " " << student.second << el;
    // auto [name, roll] = student;
    // cout << name << " " << roll << el;

    // int n;
    // cin >> n;
    // pair<string, int> students[n];
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> students[i].first >> students[i].second;
    // }
    // for (auto [name, roll] : students)
    // {
    //     cout << name << " " << roll << el;
    // }

    return 0;
}